# AUD-020.8 — P3 frontend scanner

**Estado:** Complete — awaiting approval for P4  
**Referencias:** [AUD-020.7](AUD-020.7-p2-backend.md)

## Cambios

- `lib/api.ts`: tipos `BoardingLookupDTO` / `BoardingToggleResult`; eliminado `boardPassenger`
- `lib/qr.ts`: validación exacta ticket 8 hex o QR completo
- `lib/boarding/scan.ts`: DTO helpers, `changed`, mapeo de errores a códigos de dominio
- `app/agency/scan/page.tsx`: DTO mínimo, `changed=false` silencioso, copy exacto, errores de operador

## Errores de operador

El scanner no muestra `err.message` crudo. Resuelve un `BoardingDomainCode` y muestra copy fijo (`getBoardingOperatorMessage`). Fallos internos/DB → mensaje genérico.

## Tests

- `tests/boarding/scanner-contract.test.ts`
- `tests/boarding/scanner-consumers.characterization.test.ts`
