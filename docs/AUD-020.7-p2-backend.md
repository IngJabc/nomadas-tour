# AUD-020.7 — P2 backend boarding contract

**Tipo:** Implementación backend  
**Fecha:** 2026-08-04  
**Estado:** Complete — awaiting approval to start P3 (frontend)  
**Referencias:** [AUD-020.4](AUD-020.4-implementation-preflight.md), [AUD-020.6](AUD-020.6-p1-db-foundation.md)

---

## Cambios

| Área | Resultado |
|------|-----------|
| `toggleBoarding` | Llama RPC `boarding_toggle`; mapea `changed` / conteos / status |
| `lookupPassengerByQR` | Exacto `ticket_code` / `qr_code`; DTO mínimo; sin ILIKE |
| `boarding_attempts` | Telemetría best-effort con `credential_hash` SHA-256 |
| Legacy HTTP | Eliminado (`/reservations/board`, `/scanner/*`) |

## Archivos

- `backend/src/services/reservation.service.ts`
- `backend/src/services/boarding-attempts.service.ts` *(nuevo)*
- `backend/src/types/boarding.ts` *(nuevo)*
- `backend/src/controllers/reservation.controller.ts`
- `backend/src/routes/agency/index.ts`
- `backend/src/services/reservation.service.boarding.test.ts` *(reemplaza P0 characterization)*
- `backend/src/services/boarding-attempts.service.test.ts` *(nuevo)*

## No modificado

- Migraciones `043`–`046`
- Frontend / `lib/api.ts`
- ADR-001
- `create_agency_reservation` (ticket_code en altas nuevas → pendiente migración/P2b)

## Riesgos

| Riesgo | Severidad | Nota |
|--------|-----------|------|
| Frontend aún espera DTO antiguo (documentos/QR/`booker_name`) | Alta | P3 debe adaptar scanner antes/justo después del deploy backend |
| `lib/api.ts` aún define `boardPassenger` | Baja | Sin callers; ruta backend ya no existe |
| Reservas nuevas sin `ticket_code` hasta actualizar RPC de creación | Media | Lookup por `qr_code` completo sigue funcionando |
| RPC debe existir en DB antes del deploy backend | Alta | Orden: migraciones → verificar → backend |

## Pendiente P3

- Tipar API client; eliminar `boardPassenger`
- Scanner: input exacto, DTO mínimo, `changed=false`
- Preferir `POST /boarding/lookup` (QR fuera de URL) si se cierra en P3
